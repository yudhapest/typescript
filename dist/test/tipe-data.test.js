"use strict";
describe('Data Type', () => {
    it('should must declare', () => {
        let name = 'Yudha Irmawan';
        let balance = 5000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
