describe('Data Type', () => {
  it('should must declare', () => {
    let name: String = 'Yudha Irmawan';
    let balance: number = 5000;
    let isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
